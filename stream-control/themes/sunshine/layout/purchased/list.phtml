<div class="col-lg-4 col-md-6" data-purchase-id="<?php echo $wo['purchase']->id; ?>">
	<div class="wow_content my_purchased">
		<div class="id purchases-status-bar">
			<div> #<?php echo $wo['purchase']->id; ?>
			</div>

			<div>
			<?php 
				// Retrieve the purchase order status from the wo_userorders table
				$purchase_status = $db->where('hash_id', $wo['purchase']->order_hash_id)->getValue(T_USER_ORDERS, 'status');
				
				// Display the purchase status
				if (!empty($purchase_status)) {
					echo $wo['lang']['status'] . ': <span class="badge">' . ucfirst($wo['lang'][$purchase_status]) . '</span>';
					
					// If the status is 'shipped', display a dropdown to change the status
					if ($purchase_status == 'shipped') { ?>
						<select class="border-bottom" id="cart_product_qty" onchange="ChangeStatus(this, '<?php echo $wo['purchase']->order_hash_id; ?>')">
							<option value="shipped" <?php if ($purchase_status == 'shipped') { echo "selected"; } ?>><?php echo $wo['lang']['shipped']; ?></option>
							<option value="delivered" <?php if ($purchase_status == 'delivered') { echo "selected"; } ?>><?php echo $wo['lang']['delivered']; ?></option>
						</select>
					<?php }
				} else {
					echo 'Status not available'; // Fallback if status is not found
				}
			?>

			</div>
				
			<style>
			.id.purchases-status-bar {
				display: flex;
				justify-content: space-between;
			}
			</style>

		</div>
		<a href="<?php echo $wo['purchase']->url; ?>" class="title"><?php echo $wo['purchase']->data['name']; ?></a>
		
		
		<div class="price">
		<?php echo 'PID'; ?>: <?php echo isset($wo['purchase']->data['product_id']) ? $wo['purchase']->data['product_id'] : 'null'; ?>,
		<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"><path fill="currentColor" d="M11,13.5V21.5H3V13.5H11M12,2L17.5,11H6.5L12,2M17.5,13C20,13 22,15 22,17.5C22,20 20,22 17.5,22C15,22 13,20 13,17.5C13,15 15,13 17.5,13Z"></path></svg>

		<?php echo $wo['lang']['qty']; ?>: <?php echo isset($wo['purchase']->data['units']) ? $wo['purchase']->data['units'] : 'null'; ?>,

			<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M22 12C22 17.52 17.52 22 12 22C6.48 22 2 17.52 2 12C2 6.48 6.48 2 12 2C17.52 2 22 6.48 22 12Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path class="icon_main" d="M15.7099 15.1798L12.6099 13.3298C12.0699 13.0098 11.6299 12.2398 11.6299 11.6098V7.50977" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>


			<?php 
		if (isset($wo['purchase']->time)) {
			$purchase_time = $wo['purchase']->time;
			$current_time = time();
			$time_difference = $current_time - $purchase_time;

			// Calculate time elapsed
			if ($time_difference < 60) {
				$time_elapsed = $time_difference . ' seconds ago';
			} elseif ($time_difference < 3600) {
				$time_elapsed = floor($time_difference / 60) . ' minutes ago';
			} elseif ($time_difference < 86400) {
				$time_elapsed = floor($time_difference / 3600) . ' hours ago';
			} elseif ($time_difference < 604800) {
				$time_elapsed = floor($time_difference / 86400) . ' days ago';
			} else {
				$time_elapsed = date('Y/m/d g:ia', $purchase_time); // Fallback to formatted date
			}
			echo $time_elapsed;
		} else {
			echo 'Time not available';
		}
	?>



		</div>
		
		
		<div class="price">
			<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M17 20.5H7C4 20.5 2 19 2 15.5V8.5C2 5 4 3.5 7 3.5H17C20 3.5 22 5 22 8.5V15.5C22 19 20 20.5 17 20.5Z" stroke="currentColor" stroke-width="2" stroke-miterlimit="10" stroke-linecap="round" stroke-linejoin="round"/><path d="M12 15C13.6569 15 15 13.6569 15 12C15 10.3431 13.6569 9 12 9C10.3431 9 9 10.3431 9 12C9 13.6569 10.3431 15 12 15Z" stroke="currentColor" stroke-width="2" stroke-miterlimit="10" stroke-linecap="round" stroke-linejoin="round"/><path class="icon_main" d="M2 9H3C6 9 7 8 7 5V4" stroke-width="2" stroke-miterlimit="10" stroke-linecap="round" stroke-linejoin="round"/><path class="icon_main" d="M22 9H21C18 9 17 8 17 5V4" stroke-width="2" stroke-miterlimit="10" stroke-linecap="round" stroke-linejoin="round"/><path class="icon_main" d="M2 15H3C6 15 7 16 7 19V20" stroke-width="2" stroke-miterlimit="10" stroke-linecap="round" stroke-linejoin="round"/><path class="icon_main" d="M22 15H21C18 15 17 16 17 19V20" stroke-width="2" stroke-miterlimit="10" stroke-linecap="round" stroke-linejoin="round"/></svg> 
			
			<?php 
				// Retrieve the currency symbol
				$currency = isset($wo['purchase']->data['currency']) 
					? $wo['purchase']->data['currency']
					: (isset($wo['config']['currency_symbol_array'][$wo['config']['currency']]) 
						? $wo['config']['currency_symbol_array'][$wo['config']['currency']] 
						: 'null');

				// Retrieve the price
				$price = isset($wo['purchase']->price) ? $wo['purchase']->price : 0;

				// Retrieve the units
				$units = isset($wo['purchase']->data['units']) ? $wo['purchase']->data['units'] : 1;

				// Calculate the total
				$total = $price * $units;

				// Display the currency and total
				echo $currency . ' ' . number_format($total, 2);
			?>
	
		</div>
		<div class="id">#<?php echo $wo['purchase']->order_hash_id; ?></div>

		<div class="foot">
			<!-- <a class="btn btn-main btn-sm" href="javascript:void(0)" onclick="DownloadPurchased('<?php /* echo $wo['purchase']->order_hash_id; ?>','order')"> <?php echo $wo['lang']['download_invoice'] */ ?></a> -->
			
			<a class="btn btn-main btn-sm" href="<?php echo $wo['purchase']->url; ?>" class="title"> <?php echo $wo['lang']['details'] ?></a>

		</div>
	</div>
</div>